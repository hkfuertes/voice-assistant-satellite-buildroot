#!/bin/sh

case "$1" in
  start)
    echo "Configuring ReSpeaker 2-Mic ALSA..."
    
    alsactl restore 2>/dev/null || true
    
    echo "ReSpeaker 2-Mic ready"
    ;;
  stop)
    ;;
  *)
    echo "Usage: $0 {start|stop}"
    exit 1
    ;;
esac

exit 0
