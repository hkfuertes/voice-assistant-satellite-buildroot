# Handoff: voice-assistant-satellite-buildroot

## Repo/session state
- Repo: `/Users/hkfuertes/projects/voice-assistant-satellite-buildroot`
- Branch: `issue-lva-v1-1-12-wyoming-removal`
- Latest pushed commit: `a660631 Update Buildroot to 2026.05`
- Remote push done to `origin/issue-lva-v1-1-12-wyoming-removal`.
- Worktree clean except ignored local artifacts/secrets: generated images, `issues/`, PRD, and local WiFi config.

## What changed since previous handoff
- Updated Buildroot base to `2026.05.x`.
- Removed custom `python-numpy-wheel`; switched packages to upstream Buildroot `python-numpy`.
- Fixed Python-version hardcoding for Buildroot 2026/Python 3.14 via `$(PYTHON3_VERSION_MAJOR)`.
- Added Pi kernel tarball hash under `buildroot/external/board/raspberrypi/patches/linux/linux.hash` and wired external patch dir into Pi defconfigs.
- Added root `Makefile` with Docker/Colima targets and persistent Buildroot output cache for future builds.
- Fixed `wifi-autoconfig.mk` typo `$(@d)` -> `$(@D)`.
- Added LVA runtime logging in `buildroot/external/package/linux-voice-assistant/files/S95linux-voice-assistant.sh` to `/var/log/linux-voice-assistant.log`.

## Built/flashed/running hardware
- Locally built `buildroot/lva-wm8960hat-pi.img.xz` from Buildroot 2026.05.
- Flashed to external `/dev/disk4` manually by user after sudo issue.
- WiFi was not embedded in that built image; added `wpa_supplicant.conf` to boot partition manually before first boot.
- Pi booted and joined WiFi.

## Live device details
- Hostname: `VoiceAssistant`
- IP: `192.168.77.175`
- SSH: `root@192.168.77.175`, password `toor`
- Home Assistant appears to be `192.168.77.254` and has an established connection to LVA port `6053`.
- LVA process running:
  `/usr/bin/python3 -m linux_voice_assistant --name VoiceAssistant-3b-25-d7 --audio-output-device alsa/sysdefault`
- WM8960 playback/capture both detected by ALSA.
- User reports wake word works and speaker plays HA test voices; overall “funciona increible”.
- Logging patch was also hot-deployed to the live Pi and LVA restarted successfully. Check logs with:
  `sshpass -p toor ssh root@192.168.77.175 'tail -200 /var/log/linux-voice-assistant.log'`

## Issue progress
- Issues 01–05 were already done before this handoff.
- Issue 06 (`issues/06-runtime-validate-lva-on-wm8960-pi.md`) is now mostly validated on real hardware:
  - boot OK
  - WiFi OK
  - LVA starts OK
  - HA connection to 6053 OK
  - wake word OK per user
  - speaker output OK per user
  - ALSA mic/capture device present
- Still worth explicitly checking before marking issue 06 complete:
  - one simple end-to-end voice query returns successfully
  - service restart after logging patch still works end-to-end
  - device reboot comes back cleanly
  - optional WebRTC mic processing if desired by acceptance criteria

## Important constraints/reminders
- User previously forbade GitHub Actions validation; validate locally/on-device only.
- Do not open PR/MR unless user asks.
- Push only with explicit user approval; this session did get approval and pushed `a660631`.
- Do not commit ignored local WiFi secret file: `buildroot/external/package/wifi-autoconfig/files/wpa_supplicant.conf`.

## Next recommended work
1. Finish issue 06 runtime checklist on live Pi.
2. Commit a small issue-06 validation note/update if appropriate.
3. Then continue sequentially to:
   - `issues/07-remove-wyoming-after-lva-confirmation.md`
   - `issues/08-clean-docs-after-wyoming-removal.md`
4. Before removing Wyoming, confirm user is satisfied with LVA end-to-end behavior after reboot.

## Useful commands
```sh
# live status
sshpass -p toor ssh root@192.168.77.175 'uptime; ps w | grep -E "linux_voice_assistant|wpa_supplicant|udhcpc" | grep -v grep; netstat -an | grep 6053'

# LVA logs
sshpass -p toor ssh root@192.168.77.175 'tail -200 /var/log/linux-voice-assistant.log'

# restart LVA
sshpass -p toor ssh root@192.168.77.175 '/etc/init.d/S95linux-voice-assistant restart'

# future cached build
make lva-wm8960
```

## Suggested skills next session
- `diagnose` if LVA/HA voice pipeline hangs again; now logs are available.
- `context-mode` for long device/build logs.
- `ponytail` is active and appropriate for Wyoming removal: delete the minimum, keep the working LVA path boring.
